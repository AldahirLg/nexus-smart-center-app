class DeviceModel {
  final int id;
  final String uid;
  final String name;
  final String type;

  const DeviceModel({
    required this.id,
    required this.uid,
    required this.name,
    required this.type,
  });
}
